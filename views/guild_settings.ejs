<%- include('header', { user: user, guild: guild, activePage: 'settings' }) %>

<div class="main-content">
    <h1>Configurações do Servidor: <%= guild.name %></h1>
    
    <div class="alert alert-info">
        Você está na página principal de configurações. Use o menu lateral para alternar entre: **Configurações Gerais**, **Comandos** e **Logs de Eventos**.
    </div>

    <h2>Auto-Role</h2>
    <p>Use os formulários aqui para configurar o auto-role e as notificações de entrada/saída.</p>
    
    <form id="autorole-form" method="POST" action="/dashboard/<%= guild.id %>/autorole">
        <label for="role-select">Cargo de Auto-Role:</label>
        <select id="role-select" name="roleId">
            <option value="none" <%= currentAutoroleId === 'none' ? 'selected' : '' %>>Desativado</option>
            <% roles.forEach(role => { %>
                <option value="<%= role.id %>" <%= currentAutoroleId === role.id ? 'selected' : '' %>>
                    @<%= role.name %>
                </option>
            <% }); %>
        </select>
        <button type="submit">Salvar Auto-Role</button>
    </form>
    
    </div>

<%- include('footer') %>