<%- include('header', { user: user, guild: guild, activePage: 'settings' }) %>

<div class="main-content">
    <h1>Configurações Gerais do Servidor: <%= guild.name %></h1>
    
    <h2>Auto-Role</h2>
    <form method="POST" action="/dashboard/<%= guild.id %>/save">
        <label for="autoroleId">Cargo de Auto-Role:</label>
        <select id="autoroleId" name="autoroleId">
            <option value="none" <%= currentAutoroleId === 'none' || !currentAutoroleId ? 'selected' : '' %>>Desativado</option>
            <% roles.forEach(role => { %>
                <option value="<%= role.id %>" <%= currentAutoroleId === role.id ? 'selected' : '' %>>
                    @<%= role.name %>
                </option>
            <% }); %>
        </select>
        <button type="submit" class="btn btn-primary">Salvar</button>
    </form>
    
    <hr>
    
    <h2>Mensagens de Entrada e Saída</h2>
    <p>Adicione os formulários para configurar as notificações de Join/Leave aqui.</p>
</div>

<%- include('footer') %>