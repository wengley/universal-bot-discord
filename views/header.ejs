<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= guild ? `${guild.name} - Painel` : 'Painel de Controle' %></title>
    <link rel="stylesheet" href="/css/styles.css"> 
</head>
<body>
    <div id="wrapper">
        <nav id="sidebar">
            <div class="sidebar-header">
                <h3>Discord Bot</h3>
            </div>
            <ul class="list-unstyled components">
                <li>
                    <a href="/dashboard">Início</a>
                </li>
                
                <% if (guild) { %>
                <li class="<%= activePage === 'settings' ? 'active' : '' %>">
                    <a href="/dashboard/<%= guild.id %>">Configurações Gerais</a>
                </li>
                <li class="<%= activePage === 'config' ? 'active' : '' %>">
                    <a href="/dashboard/<%= guild.id %>/config">Comandos</a>
                </li>
                <li class="<%= activePage === 'events' ? 'active' : '' %>">
                    <a href="/dashboard/<%= guild.id %>/events">Logs de Eventos</a>
                </li>
                <% } %>
            </ul>
        </nav>
        
        <div id="content">
            <div class="user-info-bar">
                <div class="user-details-group">
                    <img src="<%= user.avatar ? `https://cdn.discordapp.com/avatars/${user.id}/${user.avatar}.png` : '/images/default-avatar.png' %>" width="40" height="40" alt="Avatar" class="user-avatar">
                    <span class="user-tag"><%= user.username %>#<%= user.discriminator %></span>
                </div>
                <a href="/logout" class="btn btn-logout">Sair</a>
            </div>
            ```

### 2. `views/footer.ejs`

```ejs
        </div> </div> </body>
</html>