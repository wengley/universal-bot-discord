<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Universal Bot | <%= (typeof guild !== 'undefined' && guild) ? guild.name : 'Dashboard' %></title>
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="icon" href="https://cdn.discordapp.com/icons/1413669247023841342/bf008aa2a004c2d25d1e3e4577e93450.webp?size=128" type="image/webp">
    <script>
        function toggleTheme() {
            document.body.classList.toggle('dark-mode');
            const isDarkMode = document.body.classList.contains('dark-mode');
            localStorage.setItem('theme', isDarkMode ? 'dark' : 'light');
        }
        document.addEventListener('DOMContentLoaded', () => {
            const savedTheme = localStorage.getItem('theme') || 'light';
            if (savedTheme === 'dark') {
                document.body.classList.add('dark-mode');
            }
        });
    </script>
</head>
<body>

<div id="wrapper">
    ```

---

### 2. Corrigir o `views/dashboard.ejs` (Garantir a Chamada Correta)

O seu `dashboard.ejs` deve ser o arquivo que chama a sequência correta: `header` -> `sidebar` -> `content` -> `footer`.

Substitua **TODO** o conteúdo do seu **`views/dashboard.ejs`** por este (usando a versão corrigida da nossa última interação):

```ejs
<%- include('header', { user: user, guild: null, activePage: 'servers', botAvatarUrl: botAvatarUrl }) %>

<%- include('sidebar', { user: user, guild: null, activePage: 'servers', botAvatarUrl: botAvatarUrl }) %>

<div id="content">
    <div class="main-content">
        <h1>Seus Servidores</h1>
        <p>Selecione um servidor abaixo para começar a configurá-lo. O Bot Universal deve ser administrador ou ter permissões suficientes.</p>

        <% if (showInviteAlert) { %>
            <div class="alert alert-danger">
                ❌ O Universal Bot precisa de permissão de Administrador (ou similar) para ser adicionado. Por favor, convide-o novamente com as permissões corretas.
            </div>
        <% } %>

        <div class="guild-grid">
            <% guilds.forEach(g => { %>
                <div class="guild-card <%= g.isInBot ? 'bot-in' : 'bot-out' %>">
                    <img src="<%= g.icon %>" alt="<%= g.name %> Logo" class="guild-icon">
                    <div class="guild-info">
                        <h2><%= g.name %></h2>
                        <p class="role-tag">Seu Cargo: <%= g.userRole %></p>
                    </div>
                    
                    <% if (g.isInBot) { %>
                        <a href="/dashboard/<%= g.id %>" class="btn btn-primary">
                            <i class="fas fa-cog"></i> Configurar
                        </a>
                    <% } else { %>
                        <a href="https://discord.com/oauth2/authorize?client_id=<%= client_id %>&permissions=8&scope=bot%20applications.commands&guild_id=<%= g.id %>&response_type=code&redirect_uri=<%= callback_url %>" 
                            target="_blank" class="btn btn-secondary">
                            <i class="fas fa-plus-circle"></i> Adicionar Bot
                        </a>
                    <% } %>
                </div>
            <% }); %>
        </div>

    </div>
</div>

<%- include('partials/footer') %>