<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Universal Bot | <%= (typeof guild !== 'undefined' && guild) ? guild.name : 'Dashboard' %></title>
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="icon" href="https://cdn.discordapp.com/icons/1413669247023841342/bf008aa2a004c2d25d1e3e4577e93450.webp?size=128" type="image/webp">
    <script>
        function toggleTheme() {
            document.body.classList.toggle('dark-mode');
            const isDarkMode = document.body.classList.contains('dark-mode');
            localStorage.setItem('theme', isDarkMode ? 'dark' : 'light');
        }
        document.addEventListener('DOMContentLoaded', () => {
            const savedTheme = localStorage.getItem('theme') || 'light';
            if (savedTheme === 'dark') {
                document.body.classList.add('dark-mode');
            }
        });
    </script>
</head>
<body>

<div id="wrapper">
    ```

---

### 2. Verificar os Outros Arquivos EJS

O problema provavelmente está no `header.ejs`, mas é bom checar que você tem a seguinte estrutura:

* **`views/header.ejs`**: (Abertura do HTML, como acima).
* **`views/sidebar.ejs`**: (A barra de navegação).
* **`views/partials/footer.ejs`**: (Fecha `div#content`, `div#wrapper`, `body` e `html`).

Se você garantir que o `header.ejs` está com o código acima, o erro de recursão será quebrado. Tente fazer o deploy com essa correção.