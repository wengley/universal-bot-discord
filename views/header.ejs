<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Universal Bot | <%= (typeof guild !== 'undefined' && guild) ? guild.name : 'Dashboard' %></title>
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="icon" href="https://cdn.discordapp.com/icons/1413669247023841342/bf008aa2a004c2d25d1e3e4577e93450.webp?size=128" type="image/webp">
    <script>
        function toggleTheme() {
            document.body.classList.toggle('dark-mode');
            const isDarkMode = document.body.classList.contains('dark-mode');
            localStorage.setItem('theme', isDarkMode ? 'dark' : 'light');
        }
        document.addEventListener('DOMContentLoaded', () => {
            const savedTheme = localStorage.getItem('theme') || 'light';
            if (savedTheme === 'dark') {
                document.body.classList.add('dark-mode');
            }
        });
    </script>
</head>
<body>

<div id="wrapper">
    ```

---

## 📄 2. Arquivo Novo: `views/sidebar.ejs`

**Instrução:** **Crie ou substitua** este arquivo. Ele contém toda a lógica de navegação que estava no seu `header.ejs`.

```ejs
<nav id="sidebar">
    <div class="sidebar-header-title">
        <img src="<%= botAvatarUrl %>" alt="Logo" class="bot-logo">
        <h1>Universal Bot</h1>
    </div>

    <ul class="list-unstyled components">
        
        <% if (guild) { %>
            <li class="<%= activePage === 'servers' ? 'active' : '' %>">
                <a href="/dashboard">Voltar</a>
            </li>
            <li class="separator">CONFIG. <%= guild.name.toUpperCase() %></li> 
            
            <li class="<%= activePage === 'settings' ? 'active' : '' %>">
                <a href="/dashboard/<%= guild.id %>">Config. Geral</a> 
            </li>
            <li class="<%= activePage === 'welcome' ? 'active' : '' %>">
                <a href="/dashboard/<%= guild.id %>/welcome">Boas-Vindas</a>
            </li>
            <li class="<%= activePage === 'autorole' ? 'active' : '' %>">
                <a href="/dashboard/<%= guild.id %>/autorole">AutoRole</a>
            </li>
            <li class="<%= activePage === 'eventlogs' ? 'active' : '' %>">
                <a href="/dashboard/<%= guild.id %>/eventlogs">Event Logs</a>
            </li>
            
        <% } else { %>
            <li class="<%= activePage === 'servers' ? 'active' : '' %>">
                <a href="/dashboard">Seus Servidores</a>
            </li>
            <li class="<%= activePage === 'updates' ? 'active' : '' %>">
                <a href="/updates">Atualizações</a>
            </li>
        <% } %>

        <li class="separator">MISCELÂNEA</li>
        <li><a href="/logout">Sair</a></li>
    </ul>
    
    <%- include('user_profile_footer', { user: user }) %>
</nav>